# Chess Platform Local Development Environment Variables
# Copy this to .env and change values as needed.
# DO NOT commit the .env file to the repository.

POSTGRES_USER=chess_dev
POSTGRES_PASSWORD=chess_dev_$(openssl rand -hex 8)
POSTGRES_DB=chess_dev
JWT_SECRET=dev-only-insecure-jwt-secret-do-not-use-in-production
