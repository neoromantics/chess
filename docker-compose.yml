services:
  chess:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    environment:
      - PORT=8080
      - DB_PATH=/app/data/chess.db
      - JWT_SECRET=development-secret-key
    restart: always
